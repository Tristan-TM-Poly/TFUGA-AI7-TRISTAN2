# Tristan Continuous Publication Reactor — sparse 4^n run n=4..9

Compact public evidence bundle for the lineage-preserving publication-reactor stress test.

Committed artifacts:
- `generation_4.json` … `generation_9.json`: frozen GenerationProtocolReceipt + GenerationReactorReceipt pairs.
- `B12_FLAGSHIP_REGISTRY.json`: priority execution-front registry.
- `GENERATION_REACTOR_RECEIPT.md`: human-readable final n=9 receipt.
- `SPARSE_MANIFEST_DIGEST.json`: digest/size receipt for the local sparse candidate manifest.

The full 1,845-line sparse candidate manifest is intentionally not committed. Its SHA-256, byte count, cohort hashes and protocol hashes preserve protocol identity while keeping the public commit compact.

Important: expected-information-gain values are deterministic scheduling proxies, not scientific evidence. Scientific tested and verified counts remain zero because this run did not execute an outcome-producing scientific experiment or independent replication.
