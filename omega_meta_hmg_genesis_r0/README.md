# Ω-META-HMG-GENESIS-T∞ — R0

Executable research prototype for a proof-carrying, regenerative meta-hypergraph system.

## Constitutional invariants

- Generated != Verified
- Generator != Judge
- Simulation != Reality
- Capability != Authority
- Complexity must pay rent

## Minimal kernel

`OBSERVE -> RESIDUALIZE -> GENERATE -> TRANSFORM -> VERIFY -> DISTILL -> REGENERATE`

The implementation is intentionally small and standard-library-only. It is a falsifiable kernel, not a claim of a universal theory.

## Run

```bash
python -m unittest discover -s tests -v
python examples/demo.py
```
