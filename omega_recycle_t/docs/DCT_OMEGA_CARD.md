# DCT-Ω Card — Ω-RECYCLE-T∞ R0.4

```yaml
id: OMEGA-RECYCLE-T-INF-R0.4
name: Structure-Preserving Recycling and Regeneration
repo_path: omega_recycle_t/
status: D
definition: >
  OAK-safe circular-recovery evidence kernel combining route optimization,
  explicit baselines, Bayesian uncertainty, calibration diagnostics, exact
  symbiosis/transport courts, provenance-bound public-data snapshots and
  inventory/LCIA adapter boundaries.
hypotheses:
  - useful structure can be represented as an explicit ablatable prior
  - exact flow optimization can expose recoverable-value losses hidden by greedy matching
  - functional-state probabilities can be evaluated by empirical calibration metrics
  - provenance-bound snapshots reduce silent data drift but do not establish truth
  - external characterization factors can be applied transparently without implying LCA certification
equation_or_model: lexicographic max flow -> min cost; J = V - C - lambda_E E - lambda_R R - X + lambda_P P + lambda_F F
code_or_calculation: omega_recycle_t/omega_recycle/
test_path: omega_recycle_t/tests/
proof_path: omega_recycle_t/docs/R04_EVIDENCE.md
risk_boundary:
  - no hazardous physical-processing instructions
  - no claim of physical law
  - calibration metrics are not causal or safety validation
  - public source metadata and hashes are not semantic validation
  - LCIA adapter does not certify lifecycle conclusions
m_minus:
  - historical greedy symbiosis has a retained counterexample
  - current transport network is bipartite and single-stage
  - current public-data layer is generic plus source catalog, not full source-specific ETL
  - current economics remain synthetic in OAKBench
next_experiment: >
  add source-specific Eurostat/EPA adapters, empirical baseline campaigns,
  multi-hop material networks and method-governed LCIA factor sets
promotion_decision: prototype
```

## Falsification targets

1. Find transport instances where the exact court disagrees with an independent solver.
2. Measure greedy symbiosis regret across empirical networks.
3. Measure calibration drift across time or asset classes.
4. Detect upstream dataset schema/unit revisions rather than silently accepting them.
5. Preserve unmatched LCIA flows and reject incompatible factor units.
6. Keep every discovered counterexample in M⁻.
