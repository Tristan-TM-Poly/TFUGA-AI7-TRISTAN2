# Ω-HYDROQUÉBEC-TRISTAN-T∞ R0.1

## Status

Executable **public/synthetic research kernel**. This is not affiliated with, endorsed by, or connected to Hydro-Québec operational systems.

## Delivered architecture

- 17-region synthetic Québec energy mirror;
- typed energy hypergraph with provenance;
- deliberately fictitious regional corridors;
- dependency-free DC power-flow baseline;
- compound hazard, outage, cascade and restoration simulator;
- deterministic world factory and intervention campaign engine;
- Pareto filtering, CVCD summaries and causal hypotheses;
- public organizational capability mirror, not an internal org chart;
- constitutional AIT parliament with reality, physics, futures, territories, OAK and security chambers;
- refusal gate for critical-infrastructure operational details;
- JSON Schemas, policies, tests, example and Python 3.10–3.13 CI.

## Run

```bash
python -m omega_hqt_t benchmark --worlds 32
python -m omega_hqt_t mirror --output /tmp/quebec-synthetic.json
python -m omega_hqt_t mirror --organization --output /tmp/hq-capabilities.json
python -m omega_hqt_t campaign --worlds 256 --output-dir generated/omega_hqt_t/campaign-256
python -m omega_hqt_t cause
pytest -q tests/test_omega_hqt_*.py
```

## Scientific claim boundary

The model compares interventions inside a synthetic fixture. It does not reproduce the real Québec power grid, validate a real investment, predict an actual outage, infer confidential infrastructure, or provide operational authority.

## Canonical loop

```text
public/synthetic evidence -> hypergraph -> scenario worlds -> simulation
-> intervention comparison -> OAK falsification -> decision package -> M-minus
```
