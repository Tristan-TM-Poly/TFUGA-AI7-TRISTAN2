# Next 16 Actions - Canon Unification

**Status OAK:** ACTIVE / EXECUTION-PLAN

These actions convert the ecosystem into a reproducible research operating system.

## 1. Minimal axioms

Create `docs/math/TFUGA_MINIMAL_AXIOMS_v0_1.md` with primitives, objects, operators, and non-certification boundaries.

## 2. OAK enforcement

Add a script that scans markdown files for claims and requires OAK status tags.

## 3. Claims expansion

Expand `claims/claims_registry.csv` from 10 seed claims to 64 structured claims.

## 4. DCT++ schema

Create JSON schema for Document, Code, Test, Data, Risk, Ethics, Status, Next, Links.

## 5. HGFM schema

Create JSON schema for nodes, hyperedges, weights, status, evidence, and links.

## 6. First certified math cell

Prove `E_n(L_n(x)) = x` on valid domains for the Trace-Det LogExp module.

## 7. FFWT toy model

Implement a 1D signal transform example and classify its status as SIMULATED.

## 8. AIT-Primes module

Implement `prime(n)`, `pi(x)`, tests, and benchmark report.

## 9. Fractal LC toy simulator

Implement a small LC network comparison against standard LC behavior.

## 10. GAIA-SAT assumptions table

Create a scenario table that separates simulated impact from revenue or investment claims.

## 11. Ark-Reactor low-power simulator

Create an energy-routing toy model with safety constraints.

## 12. FailureSynth database

Create append-only JSONL for failed claims, contradictions, bad assumptions, and lessons.

## 13. Repository index

Add links from the root canon to all specialized repositories and their roles.

## 14. Publication scaffold

Create one-page summaries for math, physics, AI, AIT, energy, and climate.

## 15. CI tests

Add tests for schemas, CSV integrity, and status vocabulary.

## 16. Pull request review

Open a PR titled `Canon unification scaffold for TFUGA AI7 AIT` and use it as the first stable integration gate.
