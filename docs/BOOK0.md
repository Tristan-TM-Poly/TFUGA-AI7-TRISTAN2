# Foundational BOOK0 — TFUGA-AI7-TRISTAN2

**Status:** R0.1 local foundational kernel  
**Role:** rigorous public/minimal canon and executable SAGE-TRISTAN surface

## 1. Mission

This repository is the compact, testable and reusable crystallization layer of
the TFUGA / SAGE-TRISTAN ecosystem.

Its local BOOK0 is intentionally smaller than the global research archive. It
contains only the concepts, contracts, executable kernels and evidence surfaces
needed to understand what this repository actually implements and verifies.

## 2. Inheritance

This BOOK0 inherits shared Tristan contracts from the root BOOK0 Ω in
`Tristan-TM-Poly/TTM-TFUGA-AI7-TRISTAN2` rather than duplicating them.

Shared inherited principles include:

```text
SharedCanon > duplicated explanation
GeneratedView != VerifiedTruth
EvidenceRef != EvidenceValidity
ALL scope != ALL mutation
GLOBAL MAX / LOCAL MIN
```

Projection identities are treated through the public opaque `P1..P16` runtime;
this file does not disclose or redefine private projection semantics.

## 3. Local thesis

A fertile structure is useful here only when it becomes increasingly:

- definable;
- testable;
- reusable;
- reproducible;
- bounded by explicit assumptions and failure modes.

The local transformation path is:

```text
raw intuition
→ formal object
→ equation / contract
→ proof or falsifiable claim
→ algorithm
→ simulation / executable kernel
→ test
→ minimal fertile canon
```

## 4. Core objects

Local primitives include, where supported by code and documentation:

- DCT++ research packets;
- HGFM knowledge/relation structures;
- PowerScore-style maturity heuristics;
- AI-7 production/verification metabolism;
- evidence and claim records;
- executable research kernels;
- OAK status, risks, limits and promotion gates;
- M+ / M- reusable success and failure memory.

These names are interfaces to work, not proofs by naming.

## 5. Code surface

The repository contains multiple executable packages and research kernels.
Their README files, schemas, examples and tests are the authoritative local
entrypoints for implementation details.

A local BOOK0 projection for a specific kernel should load only its dependency
cone instead of the entire repository.

## 6. Verification surface

A computational claim should normally expose:

```text
specification
+ executable code
+ deterministic/reproducible test
+ assumptions
+ residual/error information
+ known limits
```

Passing tests establish only what those tests cover. They do not automatically
establish scientific truth or external experimental validity.

## 7. BOOK0_MIN

For local subject `x`:

```text
BOOK0_MIN(x)
= smallest declared local + inherited dependency surface
  sufficient to understand, run, verify and bound x
```

Optional theory remains virtual until requested.

## 8. OAK boundary

Keep explicit distinctions among:

- speculative idea;
- exploratory model;
- crystallizable definition;
- local demonstration;
- canonical repository contract;
- external scientific validation.

Repository canon is not a substitute for peer review, physical experiment or
formal proof when those are required.

## 9. Relationship to the BOOK0 hypergraph

This local BOOK0 is a node in `Ω-BOOK0-PROJECTION-HYPERGRAPH-T`.

Expected structural relations include:

```text
repo.tfuga_ai7 CONTAINS book0.tfuga_ai7
book0.tfuga_ai7 INHERITS book0.omega
project/kernel DEPENDS_ON book0.tfuga_ai7
project/kernel PROJECTS_TO opaque ProjectionPath
```

The graph may calculate dependency and impact surfaces but does not authorize
publication, merge or truth promotion.

## 10. Eighth Fire minimal projection

`omega_eighth_fire_t` is a local executable projection for evaluating **verified
distributed capability** under non-compensatory constitutional gates,
anti-capture review and mandatory n+1 probes.

Its minimum dependency cone is:

```text
BOOK0_MIN(omega_eighth_fire_t)
= docs/BOOK0.md
+ omega_capability_os_t.core
+ omega_eighth_fire_t/**
+ tests/test_omega_eighth_fire_t_r01.py
+ docs/OMEGA_EIGHTH_FIRE_T_R01.md
+ .agents/skills/eighth-fire/SKILL.md
```

The projection is Tristan's personal normative/engineering architecture; it is
not an authoritative Indigenous doctrine or cultural proof. A Capability OS
contract imported as a FirePacket remains `DECLARED_NOT_MEASURED` until separate
execution and beneficiary-outcome evidence exists.

## 11. Next verified mutations

1. Generate a machine-readable local BOOK0 manifest from repository inventory.
2. Link high-value kernels to explicit BOOK0_MIN dependency cones.
3. Attach claims to tests/evidence instead of prose-only status.
4. Detect duplicated definitions against the root/public canon.
5. Keep the public repository compact by deriving views rather than copying
   global BOOK0 content.

## 12. Nonclaims

```text
README != scientific evidence
NamedTheory != theorem
PassingTest != physical validation
BOOK0_MIN != full theory
ProjectionPath != truth
LocalCanon != global completeness
```