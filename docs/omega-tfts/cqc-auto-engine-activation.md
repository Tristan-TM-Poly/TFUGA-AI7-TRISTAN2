# CQC Auto-Engine Activation Note

**Module:** TTM-TFUGA-AI7-TRISTAN2 / Omega-CQC-TFTS-CRFT  
**Status:** partially activated / OAK-gated / workflow install blocked by connector safety filter.

## What is now active

The repository now contains a lightweight executable CQC meta-operator engine:

```text
src/cqc_engine/hex_stretch_generator.py
```

It instantiates the operator chain:

```text
X_n = C_Pi o A o F_n^G o D
```

for the seed families:

```text
F_3,3 = hexagonal_crft x antenna
F_3,8 = hexagonal_crft x advanced topological/nonreciprocal candidate
```

The engine separates:

```text
radiative proxy validation
advanced/topological candidate status
CVCD report generation
```

## Initial CVCD report

A minimal report is stored at:

```text
docs/cqc_runs/cvcd_hex_n3_zigzag_minimal.json
```

The full report is generated by running:

```text
python src/cqc_engine/hex_stretch_generator.py
```

## OAK guardrails

The engine intentionally avoids claiming full topological validation. It marks advanced/topological status as candidate-only until the repository contains:

```text
BdG or real-space invariant calculation
nonzero invariant nu
positive gap E_gap
robust mode evidence
control comparison
```

## Workflow note

An automatic GitHub Actions workflow for executing the CQC engine was prepared conceptually, but the connector safety layer blocked creation of files under `.github/workflows` during this session.

Current safe fallback:

```text
manual or external workflow execution can run the Python engine
CVCD JSON reports can be committed after OAK review
future workflow installation should keep auto-commit disabled or OAK-gated
```

## Next OAK upgrades

1. Add a graph/mesh backend for actual beta values instead of proxies.
2. Add S-parameter/full-wave hooks for radiative validation.
3. Add non-fractal controls.
4. Add BdG/topological invariant module before promoting F_3,8 beyond candidate status.
5. Add CI workflow only when repository policy and permissions are explicitly safe.
