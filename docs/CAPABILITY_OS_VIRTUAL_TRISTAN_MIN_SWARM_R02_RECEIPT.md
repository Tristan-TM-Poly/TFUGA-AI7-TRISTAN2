# R0.2 Construction Receipt

Base tree started from repaired clean `main` after an accidental temporary-file mutation was reverted. The implementation adds only the minimum-swarm court, its tests, doctrine, and the existing Capability OS CI extension.

OAK note: the accidental `tmp` write to main was immediately reverted and is treated as M- process evidence; it is not hidden from the evolution record.
