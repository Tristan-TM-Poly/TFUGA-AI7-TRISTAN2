# Ω-8F Self-Hosting Regenerative OS — R0.1

## Goal

Turn the Eighth Fire of Tristan into a bounded engineering court for **verified distributed capability**.

Core objective:

`create the minimum sufficient structure that leaves the maximum durable capability with beneficiaries`

## Cultural boundary

This module is Tristan's personal normative/engineering architecture. It must not be represented as an authoritative Indigenous doctrine, as speaking for Anishinaabe peoples, or as proving a cultural interpretation.

## Runtime

`FireProposal -> hard gates -> anti-capture scan -> operational score -> mandatory n+1 probes -> EvaluationReceipt`

Hard gates are non-compensatory:

1. evidence
2. safety
3. non-domination
4. attribution/consent
5. regeneration
6. beneficiary autonomy
7. rollback/contestability

If any gate fails, the aggregate score is suppressed and the decision is `HOLD`.

If all gates pass but anti-capture flags remain, the decision is `REVIEW`.

Only a gate-clean, default-flag-clean proposal is `ELIGIBLE`.

`ELIGIBLE != PermissionToExecute`.

## Candidate operational score

`S8F = (VCG * transfer * autonomy * regeneration * reciprocity * reach) / (cost + risk + complexity + debt + capture)`

The score is a ranking heuristic after gates, not moral proof.

## n+1 saturation

Every proposal receives probes for omitted beneficiaries, hidden dependency, simpler alternatives, Goodhart/metric gaming, externalized cost, practical revocation, system disappearance, and lower-capture decentralized alternatives.

## Capability OS bridge

`Capability -> FirePacket`

The bridge preserves domain/input/output/authority contracts and explicitly labels the evidence state `DECLARED_NOT_MEASURED`.

`CapabilityDeclared != CapabilityExecuted != BeneficiaryCapabilityGain`.

## WorldState

R0.1 uses the bounded objective:

`VerifiedCapability - Dependency - CaptureExposure - IrreversibleHarm`

This is an engineering observable, not a complete welfare function.

## OAK laws

- `GatePASS != MoralTruth`
- `ELIGIBLE != PermissionToExecute`
- `ScoreUp != RealWorldCapabilityUp`
- `Transfer != Autonomy`
- `Shared != Consented`
- `Open != Safe`
- `CapabilityDeclared != CapabilityMeasured`
- `VanishingSystemPASS != UniversalIndependence`

## Next falsification

R0.2 should add prospective capability-left-behind measurements with frozen pre/post tasks, a counterfactual baseline, independent evaluator separation, and metric-mutation tests. Until then, R0.1 proves only deterministic court behavior on supplied inputs.
