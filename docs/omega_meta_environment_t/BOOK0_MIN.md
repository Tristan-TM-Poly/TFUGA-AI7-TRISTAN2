# BOOK0_MIN — Ω-META-ENVIRONMENT-TRISTAN-T∞ R0.1

This is the smallest current generative kernel intended to reconstruct the R0.1 branch.

## Irreducible intent

Represent an environmental intervention as a typed transformation with explicit state, residual routing, evidence contracts, scale boundaries, reversibility, authority and monitoring.

## Required invariants

- no residual may disappear from accounting merely because it leaves a chosen metric;
- simulation never becomes observed reality by naming;
- compensation is not restoration;
- local success is not global success;
- high irreversibility raises the evidence/authority gate;
- residual-bearing interventions require monitoring;
- affected entities are explicit;
- software validation never upgrades to scientific validation.

## Required artifacts

- `omega_meta_environment_t/core.py`
- `omega_meta_environment_t/oak.py`
- `tests/test_omega_meta_environment_t.py`
- `schemas/environmental_transformation_genome.schema.json`
- `docs/omega_meta_environment_t/CANON_R01.md`

Everything else in R0.1 is reconstructible from these contracts.

## Reconstruction test

A reconstruction passes only if:

1. all R0.1 tests pass;
2. canonical genome SHA-256 is deterministic;
3. the eight gates remain fail-closed;
4. all permanent epistemic boundaries in `CANON_R01.md` remain visible;
5. no test or fixture claims real-world environmental certification.
