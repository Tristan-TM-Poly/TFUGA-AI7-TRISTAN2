# R0.5 frontier

1. Recursive Pocklington DAG compiler that discovers admissible factor chains rather than importing them.
2. Standards-compatible ECPP certificate parser and an independently implemented verifier subset.
3. Optional GMP, FLINT, PARI/GP and `rug` backends with cross-architecture receipts.
4. Wheel and bucket residue compiler with disk-backed segmented campaigns.
5. Transparency-log consistency proofs between checkpoints and signed key-rotation statements.
6. Live, license-aware precedence connectors with source hashes, rate limits and offline replay.
7. Remote worker protocol with mutual authentication, lease fencing tokens and duplicate-result reconciliation.
8. Performance corpus separating algorithmic gain, implementation gain and hardware gain.
9. Customer-discovery ledger measuring actual demand rather than predicted market scores.
10. Human-approved registry submission packets with no automatic publication.
