# CQC Run Reports

This directory stores generated CVCD reports for CQC-TFTS-CRFT operator runs.

Current seed run:

```text
families:
  F_3,3 = hexagonal_crft x antenna
  F_3,8 = hexagonal_crft x topological_nonreciprocal candidate
operator:
  C_Pi o A o F_n^G o D
lattice:
  hexagonal
stretch:
  sx=1.2, sy=0.9
cut:
  zigzag
```

OAK note:

```text
Radiative status can be validated only as a proxy at this stage.
Topological status remains candidate-only until a BdG/real-space invariant and gap calculation exists.
```
