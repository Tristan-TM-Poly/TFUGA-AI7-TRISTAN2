name: omega-game-t-ci

on:
  pull_request:
    paths:
      - 'omega_game_t/**'
      - '.github/workflows/omega_game_t_ci.yml'
  push:
    branches:
      - main
    paths:
      - 'omega_game_t/**'
      - '.github/workflows/omega_game_t_ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11', '3.12', '3.13']
    defaults:
      run:
        working-directory: omega_game_t
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package and pytest
        run: python -m pip install --upgrade pip pytest && python -m pip install -e .

      - name: Verify console entry point
        run: omega-game --help

      - name: Run tests
        run: python -m pytest tests -q

  wheel-smoke:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: omega_game_t
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Build wheel
        run: |
          python -m pip install --upgrade pip
          rm -rf /tmp/omega-wheelhouse /tmp/omega-game-wheel-venv
          mkdir -p /tmp/omega-wheelhouse
          python -m pip wheel . --no-deps --wheel-dir /tmp/omega-wheelhouse

      - name: Install wheel outside checkout
        run: |
          python -m venv /tmp/omega-game-wheel-venv
          /tmp/omega-game-wheel-venv/bin/python -m pip install /tmp/omega-wheelhouse/omega_game_t-*.whl

      - name: Smoke installed wheel outside checkout
        working-directory: /tmp
        run: |
          /tmp/omega-game-wheel-venv/bin/omega-game --help
          /tmp/omega-game-wheel-venv/bin/omega-game oakbench --seed 1701 --max-steps 4 --layouts 3 --shards 2 --workers 1 --fairness-threshold 0.5 > /tmp/omega-oakbench.json
          /tmp/omega-game-wheel-venv/bin/python - <<'PY'
          import json
          from pathlib import Path
          payload = json.loads(Path('/tmp/omega-oakbench.json').read_text())
          assert payload['accepted'] is True
          assert payload['invariant_checks']['all_faults_detected'] is True
          assert len(payload['deterministic_receipt']) == 64
          PY

  scale-observation:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: omega_game_t
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install package
        run: python -m pip install --upgrade pip && python -m pip install -e .

      - name: Run bounded ScaleBench matrix
        run: omega-game scale-bench --matrix --seed 1901 > scale-observation.json

      - name: Validate observation contract
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          payload = json.loads(Path('scale-observation.json').read_text())
          assert payload['accepted'] is True
          assert len(payload['scenarios']) == 3
          assert all(row['accepted'] for row in payload['scenarios'])
          assert len(payload['deterministic_receipt']) == 64
          assert all(row['empirical_wall_clock_seconds'] >= 0 for row in payload['scenarios'])
          assert all(row['empirical_peak_tracemalloc_bytes'] >= 0 for row in payload['scenarios'])
          PY

      - name: Upload ScaleBench observation
        uses: actions/upload-artifact@v4
        with:
          name: omega-game-scale-observation-${{ github.sha }}
          path: omega_game_t/scale-observation.json
          retention-days: 30
