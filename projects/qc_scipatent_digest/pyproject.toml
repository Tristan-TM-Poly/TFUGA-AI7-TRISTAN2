[project]
name = "qc-scipatent-digest"
version = "0.3.0"
description = "OAK-safe Quebec science and patent digest scaffold with synthetic fixtures."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
